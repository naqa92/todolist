apiVersion: v1
kind: Service
metadata:
  name: todolist
  labels:
    app.kubernetes.io/name: todolist
spec:
  ports:
    - name: http
      port: 5000
      protocol: TCP
      targetPort: 5000
  selector:
    app.kubernetes.io/name: todolist
  type: ClusterIP
